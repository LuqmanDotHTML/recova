import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primaryLight = Color(0xFF0F6E56);
  static const primaryDark = Color(0xFF5DCAA5);
  static const primarySurfaceLight = Color(0xFFE1F5EE);
  static const primarySurfaceDark = Color(0xFF0A2E24);

  // Secondary (Accent)
  static const secondaryLight = Color(0xFFEF9F27);
  static const secondaryDark = Color(0xFFFAC775);

  // Backgrounds
  static const backgroundLight = Color(0xFFFAFAF8);
  static const backgroundDark = Color(0xFF1A1A19);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF2C2C2A);
  static const surfaceVariantLight = Color(0xFFF1EFE8);
  static const surfaceVariantDark = Color(0xFF3D3D3A);

  // Text
  static const onSurfaceLight = Color(0xFF2C2C2A);
  static const onSurfaceDark = Color(0xFFD3D1C7);
  static const onSurfaceVariantLight = Color(0xFF5F5E5A);
  static const onSurfaceVariantDark = Color(0xFFB4B2A9);

  // Borders
  static const outlineLight = Color(0xFFD3D1C7);
  static const outlineDark = Color(0xFF5F5E5A);

  // Error
  static const errorLight = Color(0xFFE24B4A);
  static const errorDark = Color(0xFFF09595);

  // Lost (coral)
  static const lostBgLight = Color(0xFFFAECE7);
  static const lostTextLight = Color(0xFF993C1D);
  static const lostBgDark = Color(0xFF4A1B0C);
  static const lostTextDark = Color(0xFFF0997B);

  // Found (teal)
  static const foundBgLight = Color(0xFFE1F5EE);
  static const foundTextLight = Color(0xFF085041);
  static const foundBgDark = Color(0xFF04342C);
  static const foundTextDark = Color(0xFF5DCAA5);

  // Pending (amber)
  static const pendingBgLight = Color(0xFFFAEEDA);
  static const pendingTextLight = Color(0xFF854F0B);
  static const pendingBgDark = Color(0xFF412402);
  static const pendingTextDark = Color(0xFFFAC775);

  // Approved (green)
  static const approvedBgLight = Color(0xFFEAF3DE);
  static const approvedTextLight = Color(0xFF3B6D11);
  static const approvedBgDark = Color(0xFF173404);
  static const approvedTextDark = Color(0xFF97C459);

  // Rejected (red)
  static const rejectedBgLight = Color(0xFFFCEBEB);
  static const rejectedTextLight = Color(0xFF791F1F);
  static const rejectedBgDark = Color(0xFF501313);
  static const rejectedTextDark = Color(0xFFF09595);

  // Recovered (blue)
  static const recoveredBgLight = Color(0xFFE6F1FB);
  static const recoveredTextLight = Color(0xFF0C447C);
  static const recoveredBgDark = Color(0xFF042C53);
  static const recoveredTextDark = Color(0xFF85B7EB);

  // Admin purple
  static const adminPurpleLight = Color(0xFF534AB7);
  static const adminPurpleDark = Color(0xFFAFA9EC);
}
